Run the rtmp server.
The iOS app should be set up to connect to the ip address where the rtmp server is running
the rtmp client python script should connect to the same server (localhost if run on the same computer